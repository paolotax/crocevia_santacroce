= content_for :left_col do
  &nbsp

.span12
  .hero-unit
    %h1= sexize_string("Benvenuto, ", "Benvenuta, ", current_user.name.split(" ")[0]) + current_user.name
    %p
      ti ringraziamo per esserti iscritto.
      %br
      Al momento non ci sono servizi disponibili
    %p 
      Entra nella
      = link_to photos_path, class: 'btn btn-warning' do
        %i.icon-picture
        Galleria
      per vedere alcune delle nostre proposte. 

  / .hero-unit
  /   %p.lead
  /     i nostri artisti...

  /   #ettore{ style: "width: 580px; margin: auto"}
  /     = image_tag asset_path('events/ettore_si_mostra.png')

  / .hero-unit
  /   %p.lead
  /     e i nostri oggetti...  

  /   = render 'photos/mosaic'