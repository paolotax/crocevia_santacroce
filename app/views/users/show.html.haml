.user-info.wrapper
  %p
    User: #{@user.name}

  = simple_form_for @user do |f|
    %p
      = f.collection_check_boxes :role_ids, Role.all, :id, :name
    %p
      Email:#{mail_to @user.email if @user.email}
    %p  
      = f.submit "Salva modifiche", class: "btn btn-primary" 
    